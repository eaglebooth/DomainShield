import{ContractBar}from"./ContractBar";import{PageHead}from"./PageHead";import{Workflow,type WorkflowMode}from"./Workflow";export function WorkflowPage({mode,kicker,title,copy,steps,id}:{mode:WorkflowMode;kicker:string;title:string;copy:string;steps:string[];id?:string}){return <><PageHead kicker={kicker} title={title} copy={copy}/><ContractBar/><section className="band"><div className="wrap workflow"><aside className="guide"><span className="kicker">Evidence path</span><ol>{steps.map((s,i)=><li key={s}><strong>{String(i+1).padStart(2,"0")}</strong> {s}</li>)}</ol></aside><Workflow mode={mode} id={id}/></div></section></>}
