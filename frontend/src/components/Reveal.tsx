"use client";import{motion,useReducedMotion}from"framer-motion";export function Reveal({children,className="",delay=0}:{children:React.ReactNode;className?:string;delay?:number}){const r=useReducedMotion();return <motion.div className={className} initial={r?false:{opacity:0,y:22}} whileInView={r?undefined:{opacity:1,y:0}} viewport={{once:true,amount:.18}} transition={{duration:.48,delay}}>{children}</motion.div>}
