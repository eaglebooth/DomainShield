import Link from"next/link";export function ContractBar(){const a=process.env.NEXT_PUBLIC_CONTRACT_ADDRESS||"";return <div className="contract"><div className="wrap contract-in"><div><strong>{a?"DomainShield V2 configured":"Contract address required"}</strong><div className="meta mono">{a||"Configure NEXT_PUBLIC_CONTRACT_ADDRESS."}</div></div><Link className="btn-alt" href="/activity">VERIFY CONTRACT</Link></div></div>}
