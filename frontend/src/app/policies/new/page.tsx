import{WorkflowPage}from"@/components/WorkflowPage";export default function P(){return <WorkflowPage mode="policy" kicker="Policy / create_policy" title="ANCHOR THE REAL BRAND DOMAIN." copy="The connected brand owner pays a native GEN premium and declares fixed STANDARD/FULL payout tiers. The contract locks the maximum coverage before issuing the policy." steps={["Confirm the reserve can cover the full tier.","Enter the brand and canonical domain.","Attach the premium and sign the policy transaction."]}/>}
