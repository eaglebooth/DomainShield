import{WorkflowPage}from"@/components/WorkflowPage";export default async function P({params}:{params:Promise<{id:string}>}){const{id}=await params;return <WorkflowPage mode="payout" id={id} kicker="Function 08 / payout_claim" title="FINALIZE THE APPROVED LEDGER." copy="Payout checks approval, reserve consistency, and available balance before marking the claim PAID." steps={["Confirm claim status APPROVED.","Review approved amount and reserve.","Finalize once and inspect Explorer."]}/>}
