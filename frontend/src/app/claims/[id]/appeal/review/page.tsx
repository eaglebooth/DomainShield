import{WorkflowPage}from"@/components/WorkflowPage";export default async function P({params}:{params:Promise<{id:string}>}){const{id}=await params;return <WorkflowPage mode="review" id={id} kicker="Function 07 / evaluate_claim (appeal)" title="REMAP THE RISK WITH NEW EVIDENCE." copy="When status is APPEAL_PENDING, evaluation reads the appeal URL and replaces the proposed outcome." steps={["Confirm APPEAL_PENDING status.","Verify the new evidence URL.","Run comparative review again."]}/>}
