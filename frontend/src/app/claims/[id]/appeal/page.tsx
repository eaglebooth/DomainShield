import{WorkflowPage}from"@/components/WorkflowPage";export default async function P({params}:{params:Promise<{id:string}>}){const{id}=await params;return <WorkflowPage mode="appeal" id={id} kicker="Function 06 / submit_appeal" title="ADD NEW DOMAIN EVIDENCE ONCE." copy="The policy owner can reopen an eligible ruling with one new evidence URL." steps={["Connect the policy owner wallet.","Provide evidence not used in the first review.","Open the single appeal and rerun review."]}/>}
